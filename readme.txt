In order to run the code python 3.8 and higher is required.

To run the code, open up terminal and cd into the folder wumpusworldAI.

Once in the folder, run the following commands.

# this will install all requirements
pip install -r requirements.txt

# these will run the main script
py main.py

or python3 main.py

the code will then execute and give an output.